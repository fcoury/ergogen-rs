meta.author: Ergogen Tests
meta.version: v9.9
points.zones.matrix:
pcbs.pcb.footprints_search_paths:
  - footprints
pcbs.pcb.footprints:
  - what: spec
    params:
      spec: mx_base.yaml
      from: MX_FROM
      to: MX_TO
